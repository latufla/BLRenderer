#include "stdafx.h"
#include "Infos.h"

using std::map;
using std::string;

std::map < std::string, ObjectInfo > Infos::infos = {
		{ CUBE, {
			CUBE
		}},
		{ GUN, {
			GUN
		} },
		{ POKEMON_TRAINER, {
			POKEMON_TRAINER
		} },
		{ SPIDERMAN_SYMBIOTE, {
			SPIDERMAN_SYMBIOTE
		} },
		{ STAN_LEE, {
				STAN_LEE
		} }
};
